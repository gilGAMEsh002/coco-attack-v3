# 阶段 03 验收记录

整理说明（2026-09-17）：本记录随阶段 03（clean 基线实验）的规划创建，**尚未开始运行**。当前进度以[阶段概述](./阶段概述.md#current-status)为准；执行过程、检查结果与用户裁定写入本文件；阶段收尾结论写入[阶段验收](./阶段验收.md)。

- 需求依据：[任务书](../../dspy重建任务书.md)、[主实施方案](<../CoCo-Attack DSPy 重建实施方案.md>)
- 基线契约：[主方案 H](<../CoCo-Attack DSPy 重建实施方案.md#baseline-contract>)
- 子任务计划：[01 基线配置与运行准备](./01-基线配置与运行准备.plan.md)、[02 基线运行与恢复](./02-基线运行与恢复.plan.md)、[03 结果汇总与核查](./03-结果汇总与核查.plan.md)
- 上游裁定：阶段 01 D01–D07、阶段 02 D01–D08（判定语义版本与 evasion/`llm_judge_rate` 观测口径见阶段 02 [§39 D08](../02-生成与评估闭环/验收记录.md#d08)）

## 0.1 裁定索引

| 编号 | 已记录的有效决定 | 影响范围 | 详细来源 |
|---|---|---|---|
| D01 | 基线根目录固定在 gitignore 的 `cocota_runs/phase03/<name>/`；`allow_dirty_worktree` 默认严格（false）、仅供开发/dry-run，正式基线必须严格通过且清单记录本次取值；共享功能缓存根解析为 `<baseline_root>/cache/functional`（`FunctionalCache` 自动嵌套 `functional-cache-v1/<stage>/`） | 子任务 01 的目录约定、版本冻结检查与缓存路径；02/03 沿用 | 本会话用户裁定（2026-09-17，实施 agent 风险 4/5/6 汇报后）；[§task-01](#task-01) |

## 0.2 执行证据索引

| 编号 | 范围与来源 | 实测结果 | 证据边界 |
|---|---|---|---|
| E01 | 子任务 01 dry-run：`prepare-baseline`/`check-baseline`（离线，无模型调用、无收费），产物 `cocota_runs/phase03/dryrun-baseline/`、`dryrun-matrix.json`、`strict-matrix.json`；测试 343 passed | prepare-baseline exit 0：24 单元/30 运行、victim 请求合计 1,962（486/72/594/810，与主方案 H 一致）；check-baseline exit 0；严格模式（`allow_dirty_worktree=false`）因工作树脏阻塞，符合 plan §5.2.5 | 离线 dry-run，不证明真实 API、Docker 执行或正式基线；dry-run 产物不是正式基线快照 |
| E02 | 阶段 03 当前镜像的执行隔离复检：`check-execution`（`configs/execution.local.json` + `cocota_runs/phase02/audit`，输出 `cocota_runs/phase03/check-execution/`）、`verify-isolation`（同 profile，输出 `cocota_runs/phase03/verify-isolation/`）、`docker inspect coco-attack-evaluator:dev`（2026-09-17） | `check-execution` exit 0、13/13 检查 pass（含 `image_identity` 解析 `sha256:1a8f1286f917c751cb968356156b449017a4ee8980719f64e6c44607c2775304`、`dependency_probe` python=3.14.7）；`verify-isolation` exit 0、8/8 探针 pass（identity/host_access/network/resources/hang_after_result/hang_ignoring_sigterm/partial_payload/wrong_identity），profile 指纹 `96315920c933…`；`docker inspect` 确认当前镜像摘要与解析值一致 | 使用 gitignore 的 dev profile（`image_id` 为零占位）与阶段 02 audit；探针为固定人工检查，不运行数据集代码、不建立功能基线；formal matrix-config 须填入该实际摘要，本地 profile 本身不是冻结产物 |
| E03 | 子任务 01 正式配置准备（严格模式，离线、无模型调用）：`prepare-baseline --matrix-config cocota_runs/phase03/matrix-DeepSeek-V3.2.json --output-dir cocota_runs/phase03/baseline-DeepSeek-V3.2`、随后 `check-baseline`（2026-09-17） | `prepare-baseline` exit 0；基线根目录含 24 单元/30 运行、victim 请求合计 1,962（486/72/594/810）、cwe078 18/9、cwe078 锁定 6 单元；`check-baseline` exit 0、Ready=True、无阻塞/无 limitation；记录 commit `09498b1f2e1c`、工作树不脏、镜像摘要 `sha256:1a8f1286…`（声明=解析）、DMX key 存在、bandit/semgrep/codeql 对 4 组合均 available+covered | 仅配置/输入快照证据，未发起任何 victim/judge 请求，不构成基线结果；正式运行由子任务 02 执行且需用户启动；模型串为 `openai/DeepSeek-V3.2`（DMX OpenAI 兼容端点，按阶段 02 教训加 `openai/` 前缀），victim/judge 同模型同价 |

<a id="task-01"></a>

## 1. 子任务 01：基线配置与运行准备（进行中）

### 1.1 实施过程（2026-09-17）

- 新增 `coco_attack/src/coco_attack/experiments/`：`matrix.py`（baseline-matrix-v1，24 单元/30 运行展开，题数契约 27/4/33/45 与 cwe078 18/9 校验）、`manifest.py`（run-manifest-v1，状态机）、`configgen.py`（逐运行 PipelineConfig，不预建 run 目录）、`baseline.py`（`prepare_baseline`/`check_baseline`）；CLI 增加 `prepare-baseline`/`check-baseline`；README 增补新入口并删除过时的 `--lock-state`、修正 `*_evasion` 的采样门旧口径为 D08 观测口径。
- 新增测试 4 个文件；全套 coco_attack 测试 343 passed（2026-09-17 复测）。
- 修复缺陷：`_validate_prepared` 原先读 `split.evaluation_ids` 判题数，但 search/holdout 模式下该字段按设计为空、完整集合在 `split.input_ids`；已修复，端到端测试由失败转为通过。
- dry-run 证据见 [E01](#task-01)（0.2 节）。

### 1.2 实施中发现的风险与处理

1. **阶段 02 D08 代码改动在 e0255f9b8 之后实施**：正式 prepare 前须提交含 `JUDGE_DETECTION_VERSION` 的提交；plan 01 §2 已明确提交哈希是编写时核查记录，冻结值以 prepare 时清单登记为准。用户裁定拆两个提交：(a) 阶段 02 D08 语义改动，(b) 阶段 03 experiments 模块与文档。
2. **Docker 镜像漂移**：当前 `coco-attack-evaluator:dev` 摘要 `sha256:1a8f1286…` 与阶段 02 记录（`abebeddd…` 等）不同。用户裁定以当前镜像为阶段 03 正式镜像：摘要写入 matrix-config，重跑 `check-execution`/`verify-isolation` 取当前摘要证据（不借旧镜像检查背书新镜像），漂移事实登记于此。功能缓存指纹含镜像摘要，旧缓存条目自然失效，属设计行为。
3. **缓存路径歧义**：plan 原文 `cache/functional/{search,holdout}/` 与 `FunctionalCache` 实际嵌套不符；按 D01 固化为缓存根 `<baseline_root>/cache/functional`。
4. **基线根目录与脏树开关**：见 D01。
5. **README 过时口径**：`*_evasion` 采样门描述已按 D08 口径修正（连同 `--lock-state` 条目）。

### 1.3 镜像与环境证据（2026-09-17）

- 以当前本地镜像 `coco-attack-evaluator:dev` 为阶段 03 正式镜像；`docker inspect` 摘要 `sha256:1a8f1286f917c751cb968356156b449017a4ee8980719f64e6c44607c2775304`。
- `check-execution`：exit 0，13/13 pass；解析镜像摘要与 inspect 一致，`dependency_probe` python=3.14.7。产物 `cocota_runs/phase03/check-execution/`。
- `verify-isolation`：exit 0，8/8 探针 pass；profile 指纹 `96315920c933e20899baf60432464da3d05077f50b209acefa1384e8ca2f5b42`。产物 `cocota_runs/phase03/verify-isolation/`。
- 证据边界见 [E02](#task-01)；正式 `matrix-config.json` 的 `image_digest` 须填上述摘要。

### 1.4 正式配置准备结果（2026-09-17）

- 用户提供的运行参数：victim/judge 均为 DMX `DeepSeek-V3.2`；`max_tokens`/`judge_max_tokens=8192`；计价 `0.00158/0.00237 CNY`，`pricing_version=dmx-rmb-2026-09-17-DeepSeek-V3.2-7.9zhe`；operator `labman`；dynamic/realism **保持关闭**（`enabled_layers=["sast","judge"]`）。
- 模型串按阶段 02 教训登记为 `openai/DeepSeek-V3.2`（DMX 为 OpenAI 兼容端点，DSPy/LiteLLM 需 `openai/` 前缀，见 [阶段 02 教训](../02-生成与评估闭环/Agents_deepseek_opencode2.md)）；victim 与 judge 同模型，故计价参数两者相同（如后续判定不同需改矩阵并重新冻结）。
- 严格模式 `prepare-baseline`（`allow_dirty_worktree=false`）与 `check-baseline` 均 exit 0，证据见 [E03](#task-01)。基线根目录：`cocota_runs/phase03/baseline-DeepSeek-V3.2/`；矩阵配置：`cocota_runs/phase03/matrix-DeepSeek-V3.2.json`。
- 本任务交付物（输入快照 + 运行清单 + 30 份单元配置 + 锁定记录 + 启动检查报告）已就绪，**等待人工核对/验收**；未发起任何收费请求，子任务 02（正式运行）需用户明确启动。
